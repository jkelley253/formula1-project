[project]
name = "backend"
version = "0.1.0"
description = "Serverless Formula 1 standings API for AWS Lambda"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.3"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
